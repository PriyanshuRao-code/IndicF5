conda create -n indicf5 python=3.12 -y
conda activate indicf5

pip install --upgrade pip
pip install -r requirements.txt
// fastapi, uvicorn, transformers, soundfile

// Create data/out and data/reference_voice
// Copy reference voices files if they exist and don't already exist in destination
// Then move prompt.json to reference_voices.json

python tts_api.py